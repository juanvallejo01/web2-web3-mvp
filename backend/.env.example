# Backend Environment Variables

# Server port
PORT=3001

# NOTE: No database, no blockchain RPC, no private keys needed for signature-based verification
# Backend only verifies signatures using ethers.js - no blockchain connection required
