# Frontend Environment Variables

# Spotify Configuration
# Mode: 'mock' (default) or 'real'
# NOTE: Spotify has paused new OAuth integrations. Use 'mock' for demos.
VITE_SPOTIFY_MODE=mock

# Only needed if using VITE_SPOTIFY_MODE=real
# Get your Client ID from: https://developer.spotify.com/dashboard/applications
# Redirect URI must be set to: http://localhost:5174/callback
# VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Backend API URL (default: http://localhost:3001)
# VITE_API_URL=http://localhost:3001
